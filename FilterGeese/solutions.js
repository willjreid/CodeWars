function gooseFilter (birds) {
  var geese = ["African", "Roman Tufted", "Toulouse", "Pilgrim", "Steinbacher"];

  let newBirds = geese.forEach(function(element) {
    geese[element] 
  }

  birds.filter(function(forEach(geese)));
  return newBirds;
};
